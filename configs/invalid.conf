events {
    worker_connections 1024 # Missing semicolon
}

http {
    server {
        listen 80;
        location / {
            root /var/www;
        # Missing closing brace for location
    }
} # This will leave the HTTP block unclosed or misaligned