server {
    listen 8080;
    root html;

    location / {
        index index.html;
    }
}

server {
    listen 8081;
    root html/;

    location / {
        index alt_index.html;
    }

    location /test {
        return 200 "api root";
    }
}
