server {
    listen 0.0.0.0:8080;
    server_name localhost;
    root html;

    location / {
        index index.html;
    }

    location /uploads {
        limit_except GET POST;
        upload_store var/www/uploads;
        index upload.html;
    }
}