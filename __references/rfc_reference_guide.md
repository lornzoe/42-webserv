### rfc reference guide
*ts generated by ai... 🥀*
| Feature | RFC Reference | Why? | Relevance to 42 Project |
| :--- | :--- | :--- | :--- |
| **Basic Parsing** | **RFC 1945** | Defines the simplest version of the protocol. | Use this for the initial `Request-Line` and `Header` parsing logic. |
| **Status Codes** | **RFC 9110** | The modern "Semantics" guide for status codes. | Use this to ensure your `404`, `413`, and `501` errors follow standard behavior. |
| **Keep-Alive** | **RFC 9112** | Explains how to keep the socket open for multiple requests. | Vital for the `poll()`, `select()`, or `kqueue()` loop to handle persistent connections. |
| **Host Header** | **RFC 9112** | Essential for matching requests to the correct `server { }` block. | Required to implement **Virtual Hosting** (choosing the right config based on the domain). |